# MightyBull — allow indexing of public pages; use meta tags in index.html for search snippets.
# (Google does not take the site "description" from robots.txt — it uses <meta name="description"> and page content.)

User-agent: *
Allow: /

# Auth & marketing entry points (explicit; redundant with Allow: / but documents intent)
Allow: /login
Allow: /signup
Allow: /forgot-password

Sitemap: https://mightybull.netlify.app/sitemap.xml
